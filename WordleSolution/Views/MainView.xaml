<UserControl x:Class="Wordle.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wordle"
             xmlns:localCtrl="clr-namespace:Wordle.Controls"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d" SnapsToDevicePixels="True" UseLayoutRounding="True"
             d:DesignHeight="450" d:DesignWidth="800" AllowDrop="True" Background="White"
             prism:ViewModelLocator.AutoWireViewModel="True">
    
    <StackPanel>
        <localCtrl:WordleLineStackPanel prism:RegionManager.RegionName="{x:Static local:WellknownRegionNames.WordleLinesRegion}"/>
        <ContentControl prism:RegionManager.RegionName="{x:Static local:WellknownRegionNames.WordleKeypadRegion}"/>
        <Button Content="{Binding ButtonTitle, FallbackValue=ButtonTitle}" Padding="4 12" Command="{Binding StartCommand, Mode=OneTime}"/>
    </StackPanel>
</UserControl>
